using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerStats : CharacterStats
{
    // Start is called before the first frame update
    /*void Start()
    {
        EquipmentManager.instance.onEquipmentChanged += OnEquipmentChanged;
    }

    void OnEquipmentChanged (Equipment newItem, Equipment oldItem)
    {
        if(newItem != null)
        {
            armourClass.AddModifier(newItem.armourModifier);
            damageLeftHand.AddModifier(newItem.leftHandWeaponModifier);
            damageRightHand.AddModifier(newItem.rightHandWeaponModifier);
        }

        if(oldItem != null)
        {
            armourClass.RemoveModifier(oldItem.armourModifier);
            damageLeftHand.RemoveModifier(oldItem.leftHandWeaponModifier);
            damageRightHand.RemoveModifier(oldItem.rightHandWeaponModifier);
        }
        
    }*/
}
